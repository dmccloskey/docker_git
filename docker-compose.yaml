version: "3"

services:
  git:
    image: dmccloskey/docker-git
    volumes:
      - //C/Users/domccl/GitHub/repositories.csv:/home/user/repositories.csv
    restart: always
    networks:
      - git
    
networks:
  git_network:
    ipam:
      config:
        - subnet: 172.23.0.0/16